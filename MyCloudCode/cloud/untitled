exports.getStartingThursday = function() {
    var moment = require('moment');
    var d = new Date();
    if (d.getDay() < 4) {
        return moment().startOf('week').subtract('days', 10);
    } else {
        return moment().startOf('week').subtract('days', 3);
    }
}